# دليل إعداد قاعدة البيانات

## 📋 ملخص ما فهمته عن النظام

### النظام: **نظام توزيع المياه للمشتركين (إيليا)**

هو نظام متكامل لإدارة:
1. **المشتركين (Clients)** - العملاء الذين يشتركون في خدمة توزيع المياه
2. **الموزعين (Distributors)** - الأشخاص المسؤولين عن التوزيع وجمع المدفوعات
3. **السائقين (Drivers)** - السائقين الذين يقومون بالتوصيل
4. **التوصيلات (Deliveries)** - سجلات التوصيلات اليومية (قوارير ممتلئة/فارغة، مدفوعات)
5. **المدن (Cities)** - المدن التي يتم التوزيع فيها
6. **أنواع الاشتراكات** - مثل: اشتراك شهري (30 يوم)
7. **حالات الاشتراك** - نشط، ملغي، منتهي، إلخ
8. **السحوبات النقدية** - سحوبات الموزعين من الرصيد

### الوظائف الرئيسية:
- ✅ تسجيل التوصيلات اليومية (قوارير + مدفوعات)
- ✅ تتبع رصيد الموزعين (دفعات - سحوبات)
- ✅ تتبع رصيد القوارير عند كل مشترك
- ✅ تتبع الموقع الجغرافي للموزعين والسائقين
- ✅ تقارير المستحقات والتوصيلات
- ✅ إدارة المشتركين والموزعين من لوحة التحكم

---

## 🗄️ إعداد قاعدة البيانات على السيرفر

### الطريقة 1: استخدام phpMyAdmin

1. افتح **phpMyAdmin** على السيرفر
2. أنشئ قاعدة بيانات جديدة باسم: `sarfesak_eliyaa` (أو أي اسم تريده)
3. اختر قاعدة البيانات
4. اضغط على تبويب **Import**
5. اختر ملف `database_eliyaa.sql`
6. اضغط **Go** لبدء الاستيراد

### الطريقة 2: استخدام سطر الأوامر (Terminal/SSH)

```bash
# الاتصال بقاعدة البيانات
mysql -u username -p

# إنشاء قاعدة البيانات
CREATE DATABASE sarfesak_eliyaa CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

# الخروج من MySQL
exit;

# استيراد ملف SQL
mysql -u username -p sarfesak_eliyaa < database_eliyaa.sql
```

### الطريقة 3: استخدام Laravel Migrations (مستقبلاً)

يمكن تحويل ملف SQL إلى Migrations لسهولة الصيانة، لكن حالياً يمكن استخدام ملف SQL مباشرة.

---

## ⚙️ إعداد ملف .env

بعد استيراد قاعدة البيانات، قم بتحديث ملف `.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sarfesak_eliyaa
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

---

## ✅ التحقق من الإعداد

بعد استيراد قاعدة البيانات، تحقق من:

1. **الجداول الرئيسية موجودة:**
   - `clients` - المشتركين
   - `distributors` - الموزعين
   - `delivery` - التوصيلات
   - `cities` - المدن
   - `subscription_types` - أنواع الاشتراكات
   - `subscription_statuses` - حالات الاشتراك
   - `client_types` - أنواع العملاء
   - `client_statuses` - حالات العملاء
   - `cash_withdraws` - السحوبات
   - `drivers` - السائقين
   - `users` - المستخدمين

2. **البيانات الأساسية موجودة:**
   - المدن (18 مدينة)
   - أنواع الاشتراكات
   - حالات الاشتراك
   - أنواع العملاء
   - حالات العملاء
   - مستخدم إداري (admin@gmail.com)

3. **اختبار الاتصال:**
```bash
php artisan migrate:status
```

---

## 📝 ملاحظات مهمة

1. **اسم قاعدة البيانات الحالي:** `sarfesak_eliyaa`
   - يمكن تغييره في ملف `.env` بعد الاستيراد

2. **جدول التوصيلات:** اسمه `delivery` (وليس `deliveries`)

3. **الترميز:** قاعدة البيانات تستخدم `utf8mb4_general_ci` لدعم اللغة العربية

4. **المستخدم الإداري الافتراضي:**
   - Email: `admin@gmail.com`
   - Password: (يجب إعادة تعيينه)

---

## 🔧 بعد الإعداد

1. قم بتشغيل:
```bash
php artisan config:clear
php artisan cache:clear
```

2. قم بتعيين كلمة مرور جديدة للمستخدم الإداري من لوحة التحكم

3. اختبر API endpoints للتأكد من أن كل شيء يعمل

---

## ❓ التعديل المطلوب

يرجى تحديد التعديل البسيط الذي تريد إجراؤه في النظام.

