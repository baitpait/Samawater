# نظرة عامة على نظام توزيع المياه - إيليا

## 📋 ملخص النظام

نظام إدارة توزيع المياه للمشتركين (إيليا) هو نظام متكامل لإدارة:
- المشتركين (العملاء)
- الموزعين (Distributors)
- السائقين (Drivers)
- التوصيلات (Deliveries)
- المدفوعات والرصيد
- التقارير والإحصائيات

---

## 🗄️ هيكل قاعدة البيانات

### الجداول الرئيسية:

#### 1. **clients** - المشتركين
- `id` - المعرف
- `contract_no` - رقم العقد
- `name` - اسم المشترك
- `city_id` - المدينة
- `address` - العنوان
- `phone_one`, `phone_two` - أرقام الهاتف
- `client_type` - نوع العميل (فردي، مؤسسة، تجاري، قطاعي)
- `subscription_type_id` - نوع الاشتراك
- `subscription_status_id` - حالة الاشتراك (نشط، ملغي، منتهي، إلخ)
- `subscription_start_date` - تاريخ بدء الاشتراك
- `longitude`, `latitude` - الموقع الجغرافي
- `bottle_balance` - رصيد القوارير
- `distributor_id` - الموزع المسؤول
- `image` - صورة المشترك
- `notes` - ملاحظات

#### 2. **distributors** - الموزعين
- `id` - المعرف
- `name` - الاسم
- `phone` - الهاتف
- `username` - اسم المستخدم
- `password_hash` - كلمة المرور المشفرة
- `status` - الحالة (0 = معطل، 1 = نشط)
- `latitude`, `longitude` - الموقع الجغرافي
- `last_update` - آخر تحديث للموقع
- `notes` - ملاحظات

#### 3. **delivery** - التوصيلات
- `id` - المعرف
- `client_id` - المشترك
- `delivery_date` - تاريخ التوصيل
- `bottle_received` - عدد القوارير الممتلئة المستلمة
- `bottle_empty` - عدد القوارير الفارغة المجمعة
- `distributor_id` - الموزع
- `paymant` - المبلغ المدفوع

#### 4. **cities** - المدن
- `id` - المعرف
- `city_name` - اسم المدينة

#### 5. **subscription_types** - أنواع الاشتراكات
- `id` - المعرف
- `type_name` - اسم النوع (مثل: اشتراك شهري)
- `description` - الوصف
- `distribution_days` - عدد أيام التوزيع (مثل: 30 يوم)

#### 6. **subscription_statuses** - حالات الاشتراك
- `id` - المعرف
- `status_name` - اسم الحالة (نشط، ملغي، منتهي، إلخ)

#### 7. **client_types** - أنواع العملاء
- `id` - المعرف
- `type_name` - اسم النوع (فردي، مؤسسة، تجاري، قطاعي)

#### 8. **client_statuses** - حالات العملاء
- `id` - المعرف
- `status_name` - اسم الحالة (غير ملتزم، ملتزم إلى حد ما، جيد جدًا، مميز)
- `min_percentage`, `max_percentage` - النسبة المئوية

#### 9. **cash_withdraws** - سحوبات الموزعين
- `id` - المعرف
- `distributor_id` - الموزع
- `total_amount` - المبلغ المسحوب
- `notes` - ملاحظات

#### 10. **drivers** - السائقين
- `id` - المعرف
- `name` - الاسم
- `phone` - الهاتف
- `password` - كلمة المرور
- `city` - المدينة
- `active` - نشط/غير نشط

#### 11. **users** - المستخدمين (الإدارة)
- `id` - المعرف
- `name` - الاسم
- `email` - البريد الإلكتروني
- `password` - كلمة المرور

---

## 🔄 الوظائف الرئيسية

### 1. **إدارة المشتركين**
- إضافة/تعديل/حذف المشتركين
- تتبع رصيد القوارير
- تتبع المدفوعات
- تتبع التوصيلات
- تحديث الموقع الجغرافي

### 2. **إدارة الموزعين**
- تسجيل الدخول للموزعين (API)
- تتبع موقع الموزع (GPS)
- إدارة الرصيد المالي:
  - الدفعات من التوصيلات
  - السحوبات النقدية
  - حساب الرصيد الحالي

### 3. **إدارة التوصيلات**
- تسجيل التوصيلات (API)
- تتبع القوارير (ممتلئة/فارغة)
- تسجيل المدفوعات
- تعديل التوصيلات

### 4. **التقارير**
- تقارير المستحقات على العملاء
- تقارير أداء التوصيل
- تقارير رصيد الموزعين
- تقارير التزام العملاء

### 5. **تتبع الموقع**
- تتبع موقع الموزعين (GPS)
- تتبع موقع السائقين
- تتبع موقع المشتركين

---

## 🔐 نظام المصادقة

### للموزعين:
- تسجيل الدخول عبر API: `/api/distributor/login`
- استخدام Laravel Sanctum للتوكنات
- التحقق من حالة الحساب (نشط/معطل)

### للإدارة:
- استخدام Backpack for Laravel
- تسجيل الدخول عبر لوحة التحكم

---

## 📊 العلاقات بين الجداول

```
clients
  ├── belongsTo: city
  ├── belongsTo: subscription_type
  ├── belongsTo: subscription_status
  ├── belongsTo: distributor
  └── hasMany: deliveries

deliveries
  ├── belongsTo: client
  └── belongsTo: distributor

distributors
  ├── hasMany: deliveries (المدفوعات)
  ├── hasMany: cash_withdraws (السحوبات)
  └── hasMany: clients

cash_withdraws
  └── belongsTo: distributor
```

---

## 💰 الحسابات المالية

### للموزعين:
- **إجمالي الدفعات** = مجموع `paymant` من جدول `delivery`
- **إجمالي السحوبات** = مجموع `total_amount` من جدول `cash_withdraws`
- **الرصيد الحالي** = إجمالي الدفعات - إجمالي السحوبات

### للمشتركين:
- **إجمالي المدفوع** = مجموع `paymant` من التوصيلات
- **عدد مرات الدفع** = عدد التوصيلات التي فيها `paymant > 0`
- **رصيد القوارير** = القوارير الممتلئة - القوارير الفارغة

---

## 🛠️ التقنيات المستخدمة

- **Laravel 11** - Framework
- **Backpack for Laravel** - لوحة التحكم الإدارية
- **Laravel Sanctum** - المصادقة للـ API
- **MySQL/MariaDB** - قاعدة البيانات
- **Laravel Reverb** - WebSockets (للإشعارات الفورية)

---

## 📱 API Endpoints الرئيسية

### الموزعين:
- `POST /api/distributor/login` - تسجيل الدخول
- `POST /api/distributor/logout` - تسجيل الخروج
- `GET /api/distributors` - قائمة الموزعين
- `GET /api/distributor-balance/{id}` - رصيد الموزع

### التوصيلات:
- `POST /api/deliveries` - تسجيل توصيل جديد
- `PUT /api/delivery/{id}` - تعديل توصيل

### المشتركين:
- `GET /api/allclient` - جميع المشتركين
- `GET /api/clients-due` - المشتركين المستحقين
- `POST /api/update-client-location` - تحديث موقع المشترك

### المدن:
- `GET /api/cities` - قائمة المدن

---

## 📝 ملاحظات مهمة

1. **جدول التوصيلات** اسمه `delivery` (وليس `deliveries`)
2. **كلمة المرور للموزعين** مخزنة في حقل `password_hash`
3. **حالة الموزع** (0 = معطل، 1 = نشط)
4. **رصيد القوارير** يُحسب تلقائياً من التوصيلات
5. **الموقع الجغرافي** يُستخدم لتتبع الموزعين والسائقين

---

## 🔧 التعديلات المطلوبة

يرجى تحديد التعديل البسيط المطلوب في النظام.

