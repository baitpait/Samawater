{{-- Intentionally empty. --}}
{{-- We load all JS (jQuery/Bootstrap/CoreUI/DataTables/etc) via `config/backpack/ui.php` and `inc.scripts`. --}}
{{-- This prevents duplicate loading (the main layout includes BOTH `inc.scripts` and `inc.theme_scripts`). --}}
